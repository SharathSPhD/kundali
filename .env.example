# --- Supabase (optional — leave unset to run in local mode: no auth, profiles in localStorage) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# --- Engine API (server-side only; the Next.js route handler proxies /api/engine/* here) ---
API_BASE_URL=http://localhost:8000
