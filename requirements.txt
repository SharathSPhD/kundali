# Vercel's Python builder installs from THIS file (repo root), not
# backend/requirements.txt, even though api/index.py runs backend/app code
# (see api/index.py's sys.path hack) — that split bit us once already (the
# anthropic SDK was added to backend/requirements.txt but not here, so BYOK
# Anthropic calls 502'd on Vercel with "No module named 'anthropic'" while
# local Docker tests passed). Single-source it here instead of trying to
# keep two files in sync by hand.
-r backend/requirements.txt
